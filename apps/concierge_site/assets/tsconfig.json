{
  "compilerOptions": {
    "outDir": "../priv/static/js/",
    "sourceMap": true,
    "noImplicitAny": true,
    "lib": [
      "es2015",
      "dom"
    ],
    "module": "commonjs",
    "target": "es5",
    "jsx": "react"
  },
  "include": [
    // Only look in the admin section for TypeScript files for now
    // since this is the only place they are currently in use.
    // Feel free to broaden this include when that changes.
    "./js/admin/**/*"
  ]
}
