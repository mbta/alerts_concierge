<%= unless Enum.empty?(@subscriptions) do %>
  <div class="commuter-rail-subscriptions">
    <h6>Commuter Rail</h6>
    <%= for subscription <- @subscriptions do %>
      <%= if @render_links do %>
        <%= link(to: commuter_rail_subscription_path(@conn, :edit, subscription), class: "subscription-link") do %>
          <div class="subscription">
            <div class="subscription-icon-container">
              <div class="commuter-rail-icon subscription-icon">
                <img src="<%= static_url(@conn, "/images/commuter-rail.svg") %>" />
              </div>
            </div>
            <%= subscription_info(subscription, @station_display_names, @departure_time_map) %>
            <div class="subscription-edit-link">
              <i class="fa fa-chevron-right" aria-hidden="true"></i>
            </div>
          </div>
        <% end %>
      <% else %>
        <div class="subscription">
          <div class="subscription-icon-container">
            <div class="commuter-rail-icon subscription-icon">
              <img src="<%= static_url(@conn, "/images/commuter-rail.svg") %>" />
            </div>
          </div>
          <%= subscription_info(subscription, @station_display_names, @departure_time_map) %>
        </div>
      <% end %>
    <% end %>
  </div>
<% end %>
